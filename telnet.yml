- name: install telnet
  hosts: localhost
  tasks:
    - name: install telnet
      apt:
        name: telnet
        state: present
      become: yes 
      register: telnet_install 

    - name: comment on telnet installation
      debug:
        msg: "telnet is not installed"
      when: telnet_install is changed   
...